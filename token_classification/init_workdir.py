import os

if __name__ == "__main__":
    # create directories (assuming there is no any dirs yet inside the workspace)
    os.mkdir("data")
    os.mkdir("idtag")
    os.mkdir("results")
    os.mkdir("tensorboard_report")